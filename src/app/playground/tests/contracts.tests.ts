// Contracts tests. Filled in Phase 8 (T051 — deploy-from-Michelson) and Phase 11 (T062). Empty for the MVP increment.

import { TestDefinition } from './test-types'

export const CONTRACTS_TESTS: TestDefinition[] = []
